apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  # AWS ElastiCache Redis endpoint (set actual endpoint, omit port if using TLS config separately)
  redis_host: "master.votingappdev-redis.r0rtqe.use1.cache.amazonaws.com"
  redis_port: "6379"
  redis_ssl: "true"  # set to false if ElastiCache not using in-transit encryption

  # AWS RDS PostgreSQL endpoint (set actual endpoint hostname)
  # IMPORTANT: Do not include port here; separate key provided below
  postgres_host: "voting-app-dev-pg.curq228s8eze.us-east-1.rds.amazonaws.com"
  postgres_port: "5432"
